/// אתחול Hive לפלטפורמת Web
/// Hive משתמש ב-IndexedDB אוטומטית בווב, לא צריך directory
Future<void> initHive() async {
  // Hive uses IndexedDB automatically on web
  // No directory configuration needed
}
