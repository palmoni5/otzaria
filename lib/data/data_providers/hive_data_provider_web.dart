/// אתחול Hive לפלטפורמת Web
/// Hive משתמש ב-IndexedDB אוטומטית בווב, לא צריך directory או אתחול
Future<void> initHive() async {
  // Hive uses IndexedDB automatically on web
  // No initialization needed - boxes can be opened directly
}
